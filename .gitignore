node_modules/

# Python
__pycache__/
*.py[cod]

# Playwright smoke / local verification captures
scripts/verify-*.png
scripts/portrait-*.png
scripts/direct-image.png
verify-*.png

# Local scratch (not deployed assets)
coot-ai-site-landscape.png

# Lighthouse reports (local verification only)
*.report.html

.vercel

# Cloned product repos (each has its own git remote); cloned on Vercel during build
products/

# Root API wrappers are tracked; do not ignore
!api/
!api/**
